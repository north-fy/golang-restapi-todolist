env:

server:
  port: 8080
  timeout:

postgres:
  host: localhost
  username: postgres
  password: postgres
  dbname:
  port:

redis:
  host: localhost
  port: 6379
  user:
  password:
  db: 0
  ttl: 60s
